version: "2"
services:
  nginx:
    build: ./requirements/nginx
    image: nginx
    ports:
      - 443:443
    volumes:
      - myapp:/wordpress
    depends_on:
      - wordpress
  mariadb:
    build: ./requirements/mariadb
    image: mariadb
    volumes:
      - myapp:/wordpress
  wordpress:
    build: ./requirements/wordpress
    image: wordpress
    volumes:
      - myapp:/wordpress
    depends_on:
      - mariadb
volumes:
  myapp:
networks:
  default:
